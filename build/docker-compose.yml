version: '3'

services:
  trainload:
    build: 
        context: ../
        dockerfile: ./build/Dockerfile
        args:
          NODE_VERSION: 18.17.0
          PROJECT_NAME: TrainLoad.Web
    ports:
    - "5000:80"
  
  
  
    
