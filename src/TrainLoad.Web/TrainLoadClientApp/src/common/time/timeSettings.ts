const startTime = 0;
const deltaTime = 0.001;
const deltaTimeResults = 1;

export class TimeSettings {
    startTime = startTime;
    endTime: number;
    deltaTime = deltaTime;
    deltaTimeResults = deltaTimeResults;
}
